# Tarefas de Implementação

- [x] 1. Atualizar `AssetTypeFilters` com suporte a `administrationId` e criar teste unitário.
- [x] 2. Atualizar `LegacyAssetTypeBrowserServiceInterface` e `LegacyAssetTypeBrowserService` para suportar `administrationOptions()` e filtro por administração, com testes unitários.
- [x] 3. Atualizar `LegacyAssetTypeController` para repassar administrações à view.
- [x] 4. Atualizar `resources/views/asset-types/index.blade.php` com busca rápida e seletor filtrável de administração.
- [x] 5. Adicionar e atualizar testes de feature garantindo compatibilidade e funcionamento da listagem.
- [x] 6. Validar com suíte completa de testes e `openspec validate`.
