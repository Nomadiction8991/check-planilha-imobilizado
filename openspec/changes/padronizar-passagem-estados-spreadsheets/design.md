# Decisão Técnica

Injetar `'states' => (array) config('brazil.states', [])` no array retornado pela view `spreadsheets.import` no método `create()` de `SpreadsheetImportController`.

Garantir compatibilidade e testes automatizados.
