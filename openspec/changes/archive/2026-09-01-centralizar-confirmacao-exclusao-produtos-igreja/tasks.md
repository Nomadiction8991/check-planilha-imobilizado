## 1. Testes Automatizados (TDD)

- [x] 1.1 Adicionar teste em `LegacyChurchManagementTest` verificando a presença do atributo declarativo `data-confirm` no formulário de exclusão de produtos por igreja.

## 2. Implementação e Padronização de View

- [x] 2.1 Atualizar `resources/views/churches/index.blade.php` com o atributo `data-confirm` no formulário e remover o bloco `<script>` ad-hoc inline.
- [x] 2.2 Verificar sintaxe PHP com `php -l`.

## 3. Validação e Conclusão

- [x] 3.1 Executar a suíte de testes com `php artisan test`.
- [x] 3.2 Validar conformidade do OpenSpec com `openspec validate`.
