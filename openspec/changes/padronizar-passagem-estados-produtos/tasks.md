# Tarefas de Implementação

- [x] Adicionar asserções de teste para `states` em `products.create` e `products.edit` em `LegacyProductControllerTest` <!-- id: task-test -->
- [x] Injetar `states` nos métodos `create()` e `edit()` de `LegacyProductController` <!-- id: task-controller -->
- [x] Validar com PHPUnit e php -l <!-- id: task-validate -->
