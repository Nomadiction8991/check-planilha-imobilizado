# Check Planilha Imobilizado

O sistema ativo agora está em `app/`, com Laravel como base principal.

## Como rodar

```bash
composer install
php artisan serve
```

## Observação

O código legado da raiz foi sendo removido conforme a migração avança. A implementação nova deve ficar em `app/`; o restante do repositório serve só como apoio de infraestrutura e documentação.
